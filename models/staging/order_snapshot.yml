snapshots:
  - name: orders_snapshot
    relation: ref('stg_orders')
    config:
      schema: snapshots
      database: JAFFLE_SHOP_DX
      unique_key: order_id
      strategy: timestamp
      updated_at: updated_at