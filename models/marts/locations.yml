models:
  - name: locations
    description: |
      Location dimension table. The grain of the table is one row per location.
    columns:
      - name: location_id
      - name: location_name
      - name: opened_date
